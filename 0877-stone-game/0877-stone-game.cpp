class Solution {
public:
    bool stoneGame(vector<int>& piles) {
        // sum(even indexed piles) != sum(odd indexed piles)
        // Therefore, one of these two groups must have a larger sum.
        // alice can choose any group which is sure to win..
         return true ;
    }
};